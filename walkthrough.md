# [요약] Gemini API 하부 네트워크 지연(TTFT) 분석 및 최적화

Gemini API 전송 시 발생하는 병목 지점을 측정하고, 세션 재사용, 인프라(TCP BBR), 아키텍처(컨텍스트 캐싱) 관점의 최적화 해법을 실측 데이터로 검증함.

---

## 1. 실측 데이터 (Linux VM 기준)

노이즈 없는 단일 리눅스 VM 환경에서 직접 측정한 수치.

### ① L7 연결 가속: Cold Start vs Warm Connection (10KB)
핸드셰이크 오버헤드를 추적하기 위해 10KB(텍스트)로 유효성 검증 완료.

| 회차 | Cold TTFT (초) | Warm TTFT (초) | 격차 (초) |
| :--- | :--- | :--- | :--- |
| **1회차** | 2.99s | 1.94s | -1.05s |
| **2회차** | 2.94s | 2.74s | -0.20s |
| **3회차** | 3.27s | 2.03s | -1.24s |
| **4회차** | 3.39s | 2.62s | -0.77s |
| **5회차** | 2.78s | 2.20s | -0.58s |
| **📊 평균** | **3.07s** | **2.31s** | **-0.76s (약 25% 가속)** |

> **인사이트**: gRPC/HTTP 커넥션 풀을 재사용함에 따라 순수 TCP 핸드셰이크 및 TLS 협상 시간이 약 **0.76초** 단축되는 것을 확인.

---

### ② TCP BBR vs Cubic (10MB + 패킷 유실 1%)
물리적 전송 규격 우위를 가리기 위해 1% 유실 연출 후 대형 10MB 페이로드 슛.

*   **Cubic (레거시 기본)**: **20.75초**
*   **TCP BBR (인프라 가속 활성화)**: **18.41초**
*   **📊 물리적 격차**: **약 2.34초 단축 (약 11% 성능 개선)**

> [!IMPORTANT]
> 10MB 에 달하는 거대한 데이터 전송 시, BBR이 대역폭-RTT 측정 사이클을 완전히 수렴하여 유의미한 가시적 가속 우위(11% 절감)를 보였습니다.

---

### ③ Context Caching (10MB + 인덱싱 안정화 60초)
물리적 전송 자체를 0KB로 소거했을 때의 이점.

*   Cubic (업로드 전송): **20.75초**
*   Context Caching (캐시 0KB 소거): **10.02s** (약 52% 로드타임 단축)

> **인사이트**: 10.02초는 네트워크 업로드 시간(물리 한계)이 아니라, Google TPU/GPU 가 10,000,000 글자를 처리하는 순수 연산 시간(Inference Overhead)임.

---

## 2. 요약 및 제언 (Summary)

1.  **개발 관점 (L7)**: `genai.Client()` 객체를 싱글톤으로 재사용하여 HTTP 커넥션 풀을 유도할 것.
2.  **인프라 관점 (L4)**: WAN 구간의 패킷 유실이 잦을 경우 리눅스 커널 수준에서 BBR을 우선 고려할 것.
3.  **데이터 아키텍처 관점**: 대용량 페이로드(10MB 이상)는 네트워크 물리 전송 시간 소거를 위해 Context Caching을 필수 도입할 것.
